import { EscribirchuloDirective } from './escribirchulo.directive';

describe('EscribirchuloDirective', () => {
  it('should create an instance', () => {
    const directive = new EscribirchuloDirective();
    expect(directive).toBeTruthy();
  });
});
